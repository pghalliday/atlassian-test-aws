{
  "Changes": [
  <% @hosts.each_index do |index| %>
    <%= index == 0 ? '' : ',' %>
    {
      "Action": "<%= @method %>",
      "ResourceRecordSet": {
        "Name": "<%= @hosts[index] %>.",
        "Type": "CNAME",
        "TTL": 900,
        "ResourceRecords": [{
          "Value": "<%= @public_dns_name %>"
        }]
      }
    }
  <% end %>
  ]
}
